import { readFileSync } from "node:fs";import { describe,expect,it } from "vitest";import { validOperatorBootstrapToken } from "./config";
describe("operator bootstrap boundary",()=>{it("denies missing or malformed bootstrap tokens",()=>{expect(validOperatorBootstrapToken(undefined)).toBe(false);expect(validOperatorBootstrapToken("not-a-token")).toBe(false);});it("keeps signed-in operator destination isolated",()=>{const source=readFileSync("app/auth/signed-in/page.tsx","utf8");expect(source).toContain('session.user.role==="DEMO_OPERATOR"?"/demo/backoffice"');expect(source).toContain('session.user.role==="CITIZEN"');});});
