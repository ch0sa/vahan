import { describe,expect,it } from "vitest";import { readFileSync } from "node:fs";import { backofficeError,backofficeText,outboxLabel,simulatorLabel } from "./backoffice-ui";
describe("backoffice localization",()=>{it("keeps keys aligned",()=>expect(Object.keys(backofficeText.en).sort()).toEqual(Object.keys(backofficeText.kn).sort()));it("maps controls, events, statuses and allowlisted errors",()=>{for(const locale of ["en","kn"] as const){expect(simulatorLabel(locale,"APPROVED")).not.toBe("APPROVED");expect(outboxLabel(locale,"PENDING")).not.toBe("PENDING");expect(backofficeError(locale,"invalid-event")).toBeTruthy();}});it("does not render raw internal payloads or statuses",()=>{const page=readFileSync("app/demo/backoffice/page.tsx","utf8");expect(page).not.toContain("JSON.stringify");expect(page).not.toContain("item.status.toLowerCase");expect(page).not.toContain("error.message");expect(page).toContain("paymentStatusLabel");expect(page).toContain("simulatorLabel");});it("gates follow-up events on exact delivery and re-drives deferred inbox work",()=>{const page=readFileSync("app/demo/backoffice/page.tsx","utf8"),actions=readFileSync("app/demo/backoffice/actions.ts","utf8"),service=readFileSync("src/domain/external-case-service.ts","utf8");expect(page).toContain("followupDeliveryMatches");expect(actions).toContain("retryContiguousPendingForApplication");expect(service).toContain("drainContiguousPending");});});
